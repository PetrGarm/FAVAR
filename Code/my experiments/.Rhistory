errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = "RUCBIR=ECI",
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUUNR=ECI_slow")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = "aRUCPI_slow",
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
ggseasonplot(y, polar = T)
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 2, i_name = "aRUCPI_slow",
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 3, i_name = "aRUCPI_slow",
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUUNR=ECI_slow")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 3, i_name = y_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = y_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUUNR=ECI_slow")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "RUCBIR=ECI"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = y_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUUNR=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "RUCBIR=ECI"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
Y[,"RUCBIR=ECI"]
Y[["RUCBIR=ECI"]]
Y["RUCBIR=ECI"]
colnames(Y)
print(colnames(Y))
y <- Y[,3]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "RUCBIR=ECI"
y <- Y[,3]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = "NOT",
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
res_FAVAR
res_FAVAR_unemp
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
res_FAVAR_unemp
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = y_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
res_FAVAR_unemp
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
res_FAVAR_unemp
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "RUCBIR=ECI"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
print(colnames(Y))
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUCBIR=ECI", "RUUNR=ECI_slow")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "RUCBIR=ECI"
y <- Y[,3]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUUNR=ECI_slow")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "RUCBIR=ECI"
i_name <- "aRUCPI_slow"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
colnames(ts)
autoplot(for_modeling$`RUGDP=ECI_slow`)
autoplot(for_modeling$RUGDP=ECI_slow`)
autoplot(for_modeling["RUGDP=ECI_slow"])
na.omit(for_modeling['RUGDP=ECI_slow'])
for_modeling <- read_excel("C:/Users/petrg/Desktop/Данные для диплома/real_big_data_alina.xlsx",
range = "R2C3:R242C296")
N_for_modeling <- dim(for_modeling)[2]
length(na.omit(for_modeling['RUGDP=ECI_slow']))
dim(na.omit(for_modeling['RUGDP=ECI_slow']))
#gdp
obs_gdp <- dim(na.omit(for_modeling['RUGDP=ECI_slow']))[1]
obs_gdp
for_modeling[(T-obs_gdp):]
for_modeling[(T-obs_gdp):,]
for_modeling[(T-obs_gdp):T,]
dim(for_modeling[(T-obs_gdp):T,])
dim(for_modeling[(T-obs_gdp + 1):T,])
df_gdp <- for_modeling[(T-obs_gdp + 1):T,]
df_gdp$`RUBUD=ECI_slow`
df_gdp$`RUGDP=ECI_slow`
df_gdp <- for_modeling[(70):T,]
df_gdp$`RUGDP=ECI_slow`
df_gdp <- for_modeling[(71):T,]
df_gdp$`RUGDP=ECI_slow`
dim(df_gdp$`RUGDP=ECI_slow`)
length(df_gdp$`RUGDP=ECI_slow`)
obs_gdp
dim(for_modeling)
df_gdp <- for_modeling[(71):240,]
df_gdp$`RUGDP=ECI_slow`
# Complete series only
complete_series_indices = c()
for(i in 1:N_for_modeling) {
if (sum(is.na(df_gdp[,i])) == 0) {
complete_series_indices = c(complete_series_indices, i)
}
}
complete_series_indices
length(complete())
length(complete_series_indices)
df_gdp <- df_gdp[,complete_series_indices]
dim(df_gdp)
colnames(df_gdp) <- colnames(for_modeling)[complete_series_indices]
colnames(df_gdp)
ts_gdp <- ts(df_gdp, start=c(2005,11), frequency = 12)
N_gdp <- dim(ts)[2]
T_gdp <- dim(ts)[1]
N_gdp
T_gdp
df_gdp$`RUCBIR=ECI`
df_gdp$`RUGDP=ECI_slow`
N_gdp <- dim(ts_gdp)[2]
N_gdp <- dim(ts_gdp)[2]
T_gdp <- dim(ts_gdp)[1]
N_gdp
T_gdp
transformations_gdp <- rep(0, N)
# Standardize & SA for some reason (correct PCA extraction probably)
for (i in 1:N_gdp){
ts_gdp[,i] <- (ts_gdp[,i] - mean(ts_gdp[,i])) / sd(ts_gdp[,i])
transformations_gdp[i] <- ndiffs(ts_gdp[,i])
}
mean(ts_gdp)
sd(ts_gdp)
# induce stationarity
for (i in 1:N_gdp) {
NAs <- rep(NA, transformations_gdp[i])
if(transformations_gdp[i] != 0) {
ts_gdp[,i] = c(NAs, diff(ts_gdp[,i], transformations_gdp[i]))
}
}
ts_gdp <- na.omit(ts_gdp)
dim(ts_gdp)
y_names <- c("RUGDP=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N)[colnames(ts) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
N_gdp
dim(ts_gdp)
T_gdp
y_names <- c("RUGDP=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N_gdp)[colnames(ts_gdp) %in% y_names]
y_index
ts_gdp[,12]
ts_gdp[,57]
autoplot(ts_gdp,57)
autoplot(ts_gdp[,57])
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N_gdp) %not_in% y_index)
slow_index <- seq(N)[endsWith(colnames(ts_gdp), "_slow")]
y_names <- c("RUGDP=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N_gdp)[colnames(ts_gdp) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N_gdp) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts_gdp object
slow_index_not_y
slow_index <- seq(N_gdp)[endsWith(colnames(ts_gdp), "_slow")]
y_names <- c("RUGDP=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N_gdp)[colnames(ts_gdp) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N_gdp) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts_gdp object
slow_index_not_y
y_index
Y <- ts_gdp[,y_index]
X <- ts_gdp[,x_index]
X_slow <- ts_gdp[,slow_index_not_y]
print(colnames(Y))
T_gdp <- dim(Y)[1]
### unemp
#y_names <- c("RUUNR=ECI_slow", "RUCBIR=ECI")
y_names <- c("aRUCPI_slow", "RUUNR=ECI_slow")
y_index <- seq(N)[colnames(ts) %in% y_names]
slow_index <- seq(N)[endsWith(colnames(ts), "_slow")]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts object
Y <- ts[,y_index]
X <- ts[,x_index]
X_slow <- ts[,slow_index_not_y]
print(colnames(Y))
T <- dim(Y)[1]
y_name <- "RUUNR=ECI_slow"
i_name <- "aRUCPI_slow"
y <- Y[,2]
source("my_tsCV.R")
errors_FAVAR_cv_unemp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
res_FAVAR_unemp <- sqrt(colMeans(errors_FAVAR_cv_unemp^2, na.rm = TRUE))
results_unemp <- list(FAVAR = res_FAVAR_unemp, ARIMA = res_ARIMA_unemp, ETS = res_ETS_unemp, RWD = res_rw_unemp)
results_unemp <- t(data.frame(results_unemp))
View(results_unemp)
#gdp
obs_gdp <- dim(na.omit(for_modeling['RUGDP=ECI_slow']))[1]
df_gdp <- for_modeling[(71):240,]
# Complete series only
complete_series_indices = c()
for(i in 1:N_for_modeling) {
if (sum(is.na(df_gdp[,i])) == 0) {
complete_series_indices = c(complete_series_indices, i)
}
}
df_gdp <- df_gdp[,complete_series_indices]
colnames(df_gdp) <- colnames(for_modeling)[complete_series_indices]
ts_gdp <- ts(df_gdp, start=c(2005,11), frequency = 12)
N_gdp <- dim(ts_gdp)[2]
T_gdp <- dim(ts_gdp)[1]
transformations_gdp <- rep(0, N)
# Standardize & SA for some reason (correct PCA extraction probably)
for (i in 1:N_gdp){
ts_gdp[,i] <- (ts_gdp[,i] - mean(ts_gdp[,i])) / sd(ts_gdp[,i])
transformations_gdp[i] <- ndiffs(ts_gdp[,i])
}
# induce stationarity
for (i in 1:N_gdp) {
NAs <- rep(NA, transformations_gdp[i])
if(transformations_gdp[i] != 0) {
ts_gdp[,i] = c(NAs, diff(ts_gdp[,i], transformations_gdp[i]))
}
}
ts_gdp <- na.omit(ts_gdp)
slow_index <- seq(N_gdp)[endsWith(colnames(ts_gdp), "_slow")]
y_names <- c("RUGDP=ECI_slow", "RUCBIR=ECI")
y_index <- seq(N_gdp)[colnames(ts_gdp) %in% y_names]
'%not_in%' <- Negate('%in%')
x_index <- which(seq(N_gdp) %not_in% y_index)
slow_index_not_y <- slow_index[slow_index %not_in% y_index] # w.r.t ts_gdp object
x_index
Y_gdp <- ts_gdp[,y_index]
X <- ts_gdp[,x_index]
X_slow <- ts_gdp[,slow_index_not_y]
print(colnames(Y))
print(colnames(Y_gdp))
T_gdp <- dim(Y_gdp)[1]
y_name <- "RUUNR=ECI_slow"
y_name <- "RUGDP=ECI_slow"
i_name <- "RUCBIR=ECI"
y <- Y_gdp[,2]
### forecast gdp
source("my_tsCV.R")
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 200)
T_gdp
T_gdp-24
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y, X_slow = X_slow, K = 1, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
res_FAVAR_gdp
dim(X_slow)
dim(y)
dim(Y)
dim(Y_gdp)
dim(X)
dim(X_slow)
length(y)
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 1, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
res_FAVAR_gdp
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 2, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
res_FAVAR_gdp
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 3, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
res_FAVAR_gdp
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 1, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
res_FAVAR_gdp
colnames(Y)
errors_auto_arima_cv_gdp <- tsCV(y, forecastfunction = fore_arima, h = 12,
initial = 144)
res_ARIMA_gdp <- sqrt(colMeans(errors_auto_arima_cv_gdp^2, na.rm = TRUE))
errors_auto_ets_cv_gdp <- tsCV(Y[,2], forecastfunction = fore_ets, h = 12,
initial = 144)
res_ETS_gdp <- sqrt(colMeans(errors_auto_ets_cv_gdp^2, na.rm = TRUE))
errors_rw_gdp <- tsCV(y, forecastfunction = fore_rwf, h = 12,
initial = 144)
res_rw_gdp <- sqrt(colMeans(errors_rw_gdp^2, na.rm = TRUE))
results_gdp <- list(FAVAR = res_FAVAR_gdp, ARIMA = res_ARIMA_gdp, ETS = res_ETS_gdp, RWD = res_rw_gdp)
results_gdp <- t(data.frame(results_gdp))
View(results_gdp)
errors_auto_arima_cv_gdp <- tsCV(y, forecastfunction = fore_arima, h = 12,
initial = 144)
length(y)
errors_auto_arima_cv_gdp <- tsCV(y, forecastfunction = fore_arima, h = 12,
initial = 144)
errors_auto_arima_cv_gdp <- tsCV(y, forecastfunction = fore_arima, h = 12,
initial = 144)
res_ARIMA_gdp <- sqrt(colMeans(errors_auto_arima_cv_gdp^2, na.rm = TRUE))
errors_auto_ets_cv_gdp <- tsCV(y, forecastfunction = fore_ets, h = 12,
initial = 144)
res_ETS_gdp <- sqrt(colMeans(errors_auto_ets_cv_gdp^2, na.rm = TRUE))
errors_rw_gdp <- tsCV(y, forecastfunction = fore_rwf, h = 12,
initial = 144)
res_rw_gdp <- sqrt(colMeans(errors_rw_gdp^2, na.rm = TRUE))
results_gdp <- list(FAVAR = res_FAVAR_gdp, ARIMA = res_ARIMA_gdp, ETS = res_ETS_gdp, RWD = res_rw_gdp)
results_gdp <- t(data.frame(results_gdp))
View(results_gdp)
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 2, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
results_gdp <- list(FAVAR = res_FAVAR_gdp, ARIMA = res_ARIMA_gdp, ETS = res_ETS_gdp, RWD = res_rw_gdp)
results_gdp <- t(data.frame(results_gdp))
View(results_gdp)
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 3, i_name = i_name,
initial = 144)
4 9 10 11
results_gdp <- list(FAVAR = res_FAVAR_gdp, ARIMA = res_ARIMA_gdp, ETS = res_ETS_gdp, RWD = res_rw_gdp)
results_gdp <- t(data.frame(results_gdp))
View(results_gdp)
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 4, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
results_gdp <- list(FAVAR = res_FAVAR_gdp, ARIMA = res_ARIMA_gdp, ETS = res_ETS_gdp, RWD = res_rw_gdp)
results_gdp <- t(data.frame(results_gdp))
View(results_gdp)
errors_FAVAR_cv_gdp <- my_tsCV(y = y, forecastfunction = fore_FAVAR, y_name=y_name,
h = 12, X = X, Y = Y_gdp, X_slow = X_slow, K = 1, i_name = i_name,
initial = 144)
res_FAVAR_gdp <- sqrt(colMeans(errors_FAVAR_cv_gdp^2, na.rm = TRUE))
results_gdp <- list(FAVAR = res_FAVAR_gdp, ARIMA = res_ARIMA_gdp, ETS = res_ETS_gdp, RWD = res_rw_gdp)
results_gdp <- t(data.frame(results_gdp))
View(results_gdp)
